# =============================================================
# Emotion Aware Cloud Platform — Voice Modality Dependencies
# =============================================================
# Minimal, justified dependency set for the voice processing
# pipeline. Each dependency serves a specific, documented role.
# =============================================================

# --- Scientific Computing ---
numpy>=1.24.0              # Array operations, numerical computation
pandas>=2.0.0              # Tabular data handling (feature datasets, metadata)
scipy>=1.10.0              # Signal processing utilities (resampling fallback)

# --- Audio Processing ---
librosa>=0.10.0            # Audio loading, MFCC, spectral features, pitch tracking
soundfile>=0.12.0          # WAV file I/O (backend for librosa)

# --- Machine Learning ---
scikit-learn>=1.3.0        # Classical ML models (LogisticRegression, RandomForest),
                           # preprocessing (StandardScaler), evaluation metrics,
                           # participant-level splitting (GroupKFold)
joblib>=1.3.0              # Model serialization / persistence

# --- Testing ---
pytest>=7.4.0              # Test runner for automated test suite
